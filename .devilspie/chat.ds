(if (and (is (window_property "_NET_WM_WINDOW_TYPE")
             "_NET_WM_WINDOW_TYPE_NORMAL")
         (or (and (is (application_name) "Pidgin")
                  (not (is (window_name) "Buddy List")))
             (and (is (application_name) "Skype")
                  (not (matches (window_name) "Skype.*Beta")))
             ))
    (begin (undecorate) (unmaximize)
           (geometry "963x800+0+0")))
